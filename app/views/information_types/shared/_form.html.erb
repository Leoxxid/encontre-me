      
      <%= form_for(information_type) do |f| %>
        <% if information_type.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(information_type.errors.count, "error") %> prohibited information type from being saved:</h2>

            <ul>
            <% information_type.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>
        

         <div class="field">
            <label>
              <%= f.check_box :active %>
              <span>Status</span>
            </label>
          </div>


        <div class="field">
          <%= f.label :description %>
          <%= f.text_field :description %>
        </div>
        
        <div class="actions">
            <%= f.submit "Send", class:'btn waves-effect waves-light' %>
            <i class="material-icons right">send</i>
        </div>
      <% end %>




